#include <stdio.h>
int main3423423235(void) {
	int a, b;
	printf("정수 두개:");
	scanf("%d%d",&a, &b);
		if (a > b) {
			printf("%d", a - b);
		}
		else {
			printf("%d", b - a);
		}
	return 0;
}